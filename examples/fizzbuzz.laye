fn fizzbuzz(value)
   if (var by3 = not (value % 3)) and not (value % 5)
      'fizzbuzz'
   el if by3
      'fizz'
   el if not (value % 5)
      'buzz'
   el value

(1 -> 100).forEach(fizzbuzz |> println)
